---
title: 设置
description: 缩略图与波形的开销取舍、配置文件位置，以及手动指定 ffmpeg / ffprobe 路径。
sidebar:
  label: 设置
  order: 6
---

`⌘/Ctrl+,` 打开。改动**即时生效并自动写入配置文件**，无需保存：

| 平台 | 配置位置 |
| --- | --- |
| Linux | `$XDG_CONFIG_HOME/jianjin/settings.json`（默认 `~/.config/`） |
| macOS | `~/Library/Application Support/JianJin/settings.json` |
| Windows | `%APPDATA%\JianJin\settings.json` |

## 缩略图与波形

两者可以单独关闭。它们是打开文件后最昂贵的两项分析——缩略图要解码全部关键帧，
波形要解码整条音轨。素材很大或机器较弱时关掉能立刻消除卡顿。

:::warning
关闭意味着**完全不启动对应的 ffmpeg 进程**，而不只是不显示。
:::

关闭后对应的带子整条消失、轨道随之变矮，不会留一条空白灰带。若两者都关，
轨道仍保留最小高度，片段与播放头照常可见可拖。

## 外部程序

装在非常规位置（解压版、自定义前缀、多版本共存）时，可以在这里手动指定
ffmpeg / ffprobe。界面会如实显示**当前实际会调用哪个文件**及其版本：

- 选中的文件先跑一次 `-version` 验证，跑不起来就不保存，并当场说明原因；
- 指定 ffmpeg 后会顺带认领同目录下的 ffprobe，省一次操作；
- 指定过的路径后来失效（升级、清理）时**自动回退**到自动查找，并在界面上标出来——
  不会静默失效，也不会因为一次误选就再也找不到。
